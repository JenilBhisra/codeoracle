# Backend API base URL (default local development)
VITE_API_BASE_URL=http://localhost:8000

# When true, the frontend is served entirely by src/services/mockApi.js
# with no network calls — useful for demos when the backend isn't running.
VITE_USE_MOCK_DATA=true
